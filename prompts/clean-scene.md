# Clean Scene Output

The following text is supposed to be a single scene of continuous prose. It may contain problems:

- Markdown headers (lines starting with #)
- Metadata or labels (e.g., "REVISED", "REFINEMENT", "Scene 1:", "Chapter 1:")
- Multiple scenes when only one was requested
- Commentary, notes, or variant labels from the editing process

## Scene Number

This should be Scene {scene_number} of Chapter {chapter_number} only.

## Text to Clean

{raw_scene}

## Your Task

Output ONLY the continuous prose for Scene {scene_number}. Strip all headers, metadata, commentary, and any content that belongs to other scenes. If the text contains multiple scenes, keep only Scene {scene_number}.

Do not rewrite, edit, or improve the prose. Just remove the non-prose elements.
